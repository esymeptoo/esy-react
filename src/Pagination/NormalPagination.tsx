import React, { Component } from 'react';
import { PaginationProps, PaginationState } from './Pagination'

export default class NormalPagination extends Component<PaginationProps, PaginationState> {
    render() {
        return (
            <div>normalPagination</div>
        )
    }
}