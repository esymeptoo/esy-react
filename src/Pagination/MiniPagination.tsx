import React, { Component } from 'react';
import { PaginationProps, PaginationState } from './Pagination'

export default class MiniPagination extends Component<PaginationProps, PaginationState> {
    render() {
        return (
            <div>miniPagination</div>
        )
    }
}